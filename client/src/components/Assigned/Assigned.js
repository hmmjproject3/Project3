import React from 'react'
//needs functionality for when a name is clicked it takes the chore to 
//the assigned person.
const Assgned = _ => {
    return (
        <>
            <div>
                <button id='name' onClick={}>Hamza</button>
            </div>

            <div>
                <button id='name' onClick={}>Jennifer</button>
            </div>

            <div>
                <button id='name' onClick={}>Marianna</button>
            </div>

            <div>
                <button id='name' onClick={}>Matthew</button>
            </div>


        </>
    )
}